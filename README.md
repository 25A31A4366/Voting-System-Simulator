# Voting-System-Simulator
A secure console-based Online Voting System developed in C++ that supports voter registration, login authentication, vote casting, and result tallying. It uses file handling for permanent data storage, prevents duplicate voting, and demonstrates basic password security and data structure implementation
